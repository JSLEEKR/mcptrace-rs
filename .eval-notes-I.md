# Eval Cycle I — Independent Hostile Audit (Round 80, mcptrace-rs)

## Verdict
**1 BUG FIXED → re-verify next cycle (NOT clean)**

Found a real publish-hygiene bug that prior cycles G/H missed: `cargo package` was bundling
internal harness state files (.eval-notes-A..H.md, ROUND_LOG.md) into the crates.io tarball.
Fixed by adding `package.exclude` to Cargo.toml. This is a real fix, not stylistic, so cycle
I cannot be a clean cycle by the project rules ("3 consecutive clean cycles, fresh agent").

## Required commands
1. `cargo build --release` → Finished, 0 warnings
2. `cargo test --release` → **199 passed / 0 failed** (187 unit + 12 integration + 0 doc; 1 ignored doc-test as before)
3. `cargo doc --no-deps` → exit 0
4. `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps` → exit 0

## Fresh-angle probes (different from G/H)

### Cargo.toml metadata
- name, version, edition, rust-version, authors ✓
- description, license=MIT, repository, homepage, readme, keywords (5), categories (2) ✓
- All required crates.io fields present and valid

### `cargo package --list` (BEFORE fix)
33 files including:
- .eval-notes-A.md … .eval-notes-H.md (8 internal harness files)
- ROUND_LOG.md
These would be uploaded to crates.io and visible to every user inspecting the crate.
**Bug:** internal eval/harness state leaked into the published artifact.

### Fix
Added to Cargo.toml `[package]`:
```
exclude = [".eval-notes-*.md", ".harness/", "ROUND_LOG.md"]
```

### `cargo package --list` (AFTER fix)
24 files: src/*.rs, tests/integration.rs, Cargo.toml, Cargo.lock, README, CHANGELOG, LICENSE,
.gitignore, .cargo_vcs_info.json. No internal files. Clean.

### `cargo publish --dry-run --allow-dirty` (AFTER fix)
- Packaged 24 files, 226.9KiB (58.9KiB compressed) ← was 33 files / 306.1KiB
- Verifying mcptrace v1.0.0 → compiles clean inside the packaged tarball
- "warning: aborting upload due to dry run" (expected)
- Would publish cleanly

### `cargo tree --depth 2`
Top-level deps: anyhow, clap, comfy-table, hex, reqwest (rustls, no openssl), serde, serde_json,
sha2, thiserror, tokio, toml. Dev: tempfile. No surprises, no `time`/`chrono` straggler,
no native-tls. rustls-tls correctly chosen → fully cross-platform, no system OpenSSL dep.

### Binary size
`target/release/mcptrace.exe` = 4.7 MB (release LTO=thin, codegen-units=1, strip=true).
Reasonable for a Rust CLI with reqwest+rustls+tokio+clap+comfy-table.

### LICENSE
MIT, "Copyright (c) 2026 JSLEEKR", full standard text. ✓

### CHANGELOG
v1.0.0 section present and complete (verified via git log of recent fix commits).

### `--help` walkthrough
Top-level: proxy / replay / slo / stats / help — descriptions clear.
- proxy → stdio (live), http (DEFERRED to v1.1 — errors at runtime, documented)
- slo → check
All subcommand help text is coherent and matches README claims.

### Doc-tests
Only 1 doc-test in the codebase (src/store.rs line 5), marked `ignore` — does NOT pretend to
compile. No silent broken examples. Ignored doc-test is acceptable for an illustrative snippet.

### Integration tests (12) — coverage
Cover: full proxy workflow, JSONL store roundtrip, NULL store, replay → capture exporter,
SLO p95 from real JSONL, OTLP envelope shape, Zipkin envelope shape, stdout exporter roundtrip,
JSON-RPC frame size cap, request/response correlation via id+key, **digest never leaks plaintext**,
JSONL store is LF-only. These hit every primary user flow.

## Files
- C:\claude\daily-challenge\workspace\mcptrace-rs\Cargo.toml (added `exclude`)
- C:\claude\daily-challenge\workspace\mcptrace-rs\.eval-notes-I.md (this file)

## Cycle progression
- Eval G: clean (cycle 1)
- Eval H: clean (cycle 2)
- **Eval I: 1 fix (publish hygiene) → cycle counter RESETS**
- Need 3 more consecutive clean cycles from fresh agents (J, K, L) to PASS Phase 2.

## Notes for next eval
The Cargo.toml `exclude` field needs to be re-verified by next agent via
`cargo package --list` to confirm no internal files leak. Also re-run
`cargo publish --dry-run --allow-dirty` to confirm packaging still clean.

# Eval Cycle F — mcptrace-rs

Independent hostile evaluator, fresh agent. Did not build, did not run cycles A-E.

## Toolchain
- cargo 1.94.0, rustc 1.94.0 via `/c/Users/user/.rustup/toolchains/stable-x86_64-pc-windows-msvc/bin`
- Default PATH did not contain cargo; resolved by sourcing the rustup toolchain bin directly.

## E re-verification
Commit 3c70a40: doc-only fix, ROUND_LOG.md test count 194→199 and `time = 0.3` removed from Dependencies list. Verified `git show 3c70a40 --stat` touches only ROUND_LOG.md and adds .eval-notes-E.md. Harmless.

## Probes run
1. `cargo build --release` — clean (LTO thin, strip, opt 3)
2. `cargo test --release` — 187 unit + 12 integration + 0 doc = 199 passed, 0 failed, 1 ignored
3. `mcptrace --version` → `mcptrace 1.0.0`
4. `mcptrace stats --spans <fixture>` and `--tool search` — both render comfy-table correctly
5. `mcptrace slo check` with valid clean config + clean spans → `[ok ]` × 2, exit 0
6. `mcptrace slo check` with tight burning config → `[BURN]`, exit 1
7. `mcptrace slo check` with malformed config (target=5.0 for error_rate) → clean error message, exit 1
8. `mcptrace replay <jsonl> --exporter stdout` — 3-span fixture replayed correctly
9. **Stress: 100,000 spans through `stats` and `replay`** — both complete in ~0.5s, memory bounded
10. Panic audit: every `.unwrap()` / `.expect()` / `panic!()` lives inside `#[cfg(test)]` modules. The only non-test `unreachable!()` calls in `src/main.rs` follow exhaustive `validate_*` calls and are genuinely unreachable. `proxy::finalize` uses `unwrap_or` / `unwrap_or_else` fallbacks, never `.unwrap()`.
11. `forbid(unsafe_code)` in both `src/lib.rs` and `src/main.rs` — confirmed, no `#[allow(unsafe_code)]` overrides anywhere.
12. `Cargo.lock` present and committed (good for a binary crate).
13. `Cargo.toml` deps all pinned to minor versions, matches Dependencies section in ROUND_LOG.
14. `cargo clippy` — toolchain has no clippy installed, skipped (cargo build itself runs `#![deny(warnings)]` plus `clippy::pedantic` warn-level so most drift is caught).
15. `cargo fmt --check` — toolchain has no rustfmt installed, skipped.
16. **`cargo doc --no-deps`** — INITIALLY FAILED (exit 101) with two real broken intra-doc link errors:
    - `src/error.rs:1` and `src/error.rs:5`: `[`Error`]` is ambiguous (enum + thiserror derive macro). Fix: `[`enum@Error`]`.
    - `src/store.rs:61`: `[`record`]` has no item named `record` in scope (the doc lives on `JsonlStore` impl block, not a method). Fix: `[`SpanStore::record`]`.
    Without `#![deny(warnings)]` enabling `rustdoc::broken_intra_doc_links`, this would have been a warning; with it, `cargo doc` is broken for downstream users. Eval cycles A–E never ran `cargo doc` so they missed it.

## Bugs found
**`fix(eval-F): cargo doc broken intra-doc links — error+store.rs`**
- Two ambiguous / unresolved intra-doc links broke `cargo doc --no-deps` (exit 101) due to `#![deny(warnings)]` promoting `rustdoc::broken_intra_doc_links` to a hard error.
- Fixed `src/error.rs` (lines 1 + 5) by qualifying the link as `enum@Error`.
- Fixed `src/store.rs` line 61 by qualifying the record link as `SpanStore::record`.
- Re-ran `cargo doc --no-deps` → exit 0, clean. Re-ran `cargo build --release` → clean. Re-ran `cargo test --release` → 199 passed.

## Verdict
**BUGS FIXED** in cycle F. Pushing the fix; cycle counter resets — F is fix cycle, the next 3 fresh evaluators must run clean (including `cargo doc --no-deps`) before ship.

## Notes path
.eval-notes-F.md
